# 实际开发图 · M1 核心闭环

> 对应计划：[../开发线路图/01-M1-核心闭环.md](../开发线路图/01-M1-核心闭环.md) ｜ **当前状态：🔄 进行中（T1 账务 ✅、T2 鉴权 ✅，其余未开始）**
>
> **维护方式**：每完成一个任务，把「实际产出」从 ⬜ 改为 ✅，登记真实文件路径、关键实现决策、与计划的偏差、CI run 链接。

---

## T1 账务统一入口 ✅（2026-09-14）

| 项 | 实际情况 |
|---|---|
| 状态 | ✅ 完成（L1 单测 12/12 + L2 真实 MySQL 并发 4/4 + L3 `scripts/verify/ledger.sh` 9/9 + lint/typecheck/test/build 四件套全绿）。**新手向实现详解见下方 §1.1~§1.5**；函数调用时序图见 [06-函数调用时序图.md](06-函数调用时序图.md) 第 9 图 |
| 已有代码 | 数据底座就绪：`packages/db/prisma/schema.prisma` 的 Asset/AssetFlow 表（UNIQUE(bizNo) 已建）；`packages/shared/src/bigint.ts` 精度换算直接复用 |
| 实际产出 | 数据：**AssetFlow 升级为双级余额快照**（availDelta/frozenDelta 带符号 + avail/frozen 的 Before/After 四快照 + ref 关联单号，迁移 `ledger_flow_snapshots`）；共享：`LEDGER_OPS` + `ledgerOpSchema`/`ledgerReverseSchema`；api：`src/ledger/`（ledger.service / ledger.controller / ledger.module / admin.guard）；测试：`test/ledger.spec.ts`（L1）、`test/ledger.integration.spec.ts`（L2）；脚本：`scripts/verify/ledger.sh` |
| 关键决策 | ① 一行流水记两级变化：availDelta/frozenDelta 带符号，**对账恒等式 SUM(availDelta)==Asset.available、SUM(frozenDelta)==Asset.frozen**（T9 对账直接用）② 幂等三层：事务内查 bizNo → UNIQUE(bizNo) 兜底 → 输了竞态重试后命中幂等分支返回原流水 ③ 防死锁是结构性的：事务外 `ensureAsset` 先建户，事务内只做「锁一行→校验→更新→插流水」，单一锁点无从成环 ④ 冲正=镜像原流水 delta 取反生成 REVERSAL 流水，原流水不可变，冲正同样过余额校验（不能把账冲成负）⑤ Admin 接口用 `x-admin-token` 头（ADMIN_TOKEN 环境变量），用户侧 GET /assets 挂 JwtGuard ⑥ 金额一律十进制字符串进、最小单位 bigint 算、string 出（JSON 不走 number） |
| 与计划偏差 | ① AssetFlow 表结构升级（原单快照 delta/balanceAfter → 双级快照），当时表内 0 行直接改，未留兼容字段 ② L2 集成测试用 `RUN_DB_TESTS=1` 环境变量门控，CI 暂不加 MySQL service（留给 T10 一起）③ 冻结后扣款（提现打款场景）用 `DEBIT + fromFrozen` 表达，未单列第六个操作 ④ 发现并修复 Prisma 的坑：**raw SQL 死锁报 P2010(meta.code=1213) 而非 P2034**，重试集合两者都要覆盖（见 §1.5 坑1/坑2） |

### 1.1 模块分工一览（每个功能动的是哪一层）

| 功能 | Controller | Service | 数据表 | 保护 |
|---|---|---|---|---|
| 记账四操作（入账/扣减/冻结/解冻） | POST /ledger/ops | LedgerService.apply | Asset + AssetFlow | AdminGuard（x-admin-token） |
| 差错冲正 | POST /ledger/reverse | LedgerService.reverse | AssetFlow（原流水只读） | AdminGuard |
| 我的资产查询 | GET /assets | LedgerService.assetsOf | Asset | JwtGuard（用户只看自己） |
| 流水查询（差错排查） | GET /ledger/flows | LedgerService.flowsOf | AssetFlow | AdminGuard |

> 后续 T5 撮合成交、T7 充值、T8 提现**不经过 HTTP**，直接注入 LedgerService 调 apply()——这就是「统一入口」的含义：所有资金变动只有这一扇门。

### 1.2 数据库改了什么（先动表，再写代码）

```bash
# 修改 packages/db/prisma/schema.prisma 的 AssetFlow 后执行：
cd packages/db && set -a && . ../../.env && set +a && pnpm exec prisma migrate dev --name ledger_flow_snapshots
```

AssetFlow 新结构（一行流水 = 一次操作的两级变化全记录）：

| 字段 | 含义 | 例：冻结 30 USDT |
|---|---|---|
| bizNo | 幂等单号，UNIQUE，重放返回原流水 | l3-frz-xxx |
| type | CREDIT/DEBIT/FREEZE/UNFREEZE/REVERSAL | FREEZE |
| availDelta / frozenDelta | 可用/冻结的净变化（带符号） | -30000000 / +30000000 |
| availBefore/After、frozenBefore/After | 双级余额快照（操作前后） | 100000000→70000000、0→30000000 |
| ref | 关联业务单号（冲正时=原 bizNo[:原因]） | — |

### 1.3 每个文件写了什么（按这个顺序开发即可）

| 顺序 | 文件 | 干什么 |
|---|---|---|
| ① | `packages/db/prisma/schema.prisma` | AssetFlow 双级快照化 + 迁移（见 §1.2） |
| ② | `packages/shared/src/schemas.ts` | LEDGER_OPS 四操作枚举 + ledgerOpSchema（amount 是十进制字符串）/ledgerReverseSchema 校验 |
| ③ | `src/ledger/ledger.service.ts` | **核心**。`deltasOf()` 把四操作翻译成 (availDelta, frozenDelta) 两个带符号数；`apply()`=统一入口；`transact()`=五步事务：①查 bizNo 幂等 ②FOR UPDATE 锁账户行 ③校验两级余额非负 ④更新 Asset ⑤插不可变流水；`reverse()`=镜像冲正；`ensureAsset()`=事务外建户（防死锁关键，见 §1.5）；P2002/P2034/P2010(1213) 可重试 |
| ④ | `src/ledger/admin.guard.ts` | AdminGuard：比对请求头 x-admin-token 与环境变量 ADMIN_TOKEN，不等 401 ADMIN_REQUIRED |
| ⑤ | `src/ledger/ledger.controller.ts` | 四条路由（见 §1.1）+ `toAmount()`：未知币种 400 UNKNOWN_COIN、精度超限 400 INVALID_AMOUNT（复用 toCoinBaseUnits） |
| ⑥ | `src/ledger/ledger.module.ts` | 装配；imports AuthModule（用它的 JwtGuard），exports LedgerService（给 T5/T7/T8 注入） |
| ⑦ | `src/auth/auth.module.ts` | exports 增加 `[JwtModule, JwtGuard]`——导出 guard 必须连带它依赖的 JwtModule（见 §1.5 坑3） |
| ⑧ | `src/app.module.ts` | imports 数组挂上 LedgerModule |
| ⑨ | `test/ledger.spec.ts` | L1：内存 Fake 事务（抛错回滚模拟 DB 原子性）覆盖 12 例 |
| ⑩ | `test/ledger.integration.spec.ts` | L2：真实 MySQL，`RUN_DB_TESTS=1` 才跑，否则整体 skip |
| ⑪ | `scripts/verify/ledger.sh` | L3：9 步 HTTP 全旅程（见 §1.4） |

### 1.4 每个功能怎么单独验证（起服务后逐步 curl）

前置：`pnpm --filter @cex/api dev` 起服务；金额单位=USDT 最小单位（1 USDT=1000000）。

```bash
# 0) 先造用户拿 token（流程同 auth.sh：注册→MailLog 取码→confirm→login）
TOKEN="eyJ..."   # 登录返回
LUID="用户id"     # confirm 返回的 uid，或查库
ADMIN="dev-admin-token"

# 1) 记账（管理端）：入账 100 USDT → 期望 200，flow.availAfter="100000000"
curl -s -X POST localhost:4000/ledger/ops -H 'Content-Type: application/json' -H "x-admin-token: $ADMIN" \
  -d '{"op":"CREDIT","uid":"'$LUID'","coin":"USDT","bizNo":"demo-pay-1","amount":"100"}'

# 2) 幂等：同一 bizNo 再发一遍 → 仍 200 且返回同一条流水（id 相同），余额不变

# 3) 用户查自己资产 → available="100000000", frozen="0", availableText="100"
curl -s localhost:4000/assets -H "Authorization: Bearer $TOKEN"

# 4) 冻结 30 → GET /assets 变 available="70000000", frozen="30000000"（线路图 L3 断言）
curl -s -X POST localhost:4000/ledger/ops -H 'Content-Type: application/json' -H "x-admin-token: $ADMIN" \
  -d '{"op":"FREEZE","uid":"'$LUID'","coin":"USDT","bizNo":"demo-frz-1","amount":"30"}'

# 5) 冲正入账单 → type="REVERSAL"，availDelta="-100000000"，原流水查库未变
curl -s -X POST localhost:4000/ledger/reverse -H 'Content-Type: application/json' -H "x-admin-token: $ADMIN" \
  -d '{"originalBizNo":"demo-pay-1","newBizNo":"demo-rev-1","reason":"录错"}'

# 6) 负例：DEBIT 超余额 → 422 INSUFFICIENT_BALANCE；coin:"DOGE" → 400；amount:"0.0000001" → 400
```

一键全旅程：`bash scripts/verify/ledger.sh`（9 步断言，含无令牌 401、幂等重放、快照核验、冲正后原流水不变）。
并发验证（真实 MySQL）：`cd apps/api && RUN_DB_TESTS=1 pnpm exec dotenv -e ../../.env -- pnpm exec vitest run test/ledger.integration.spec.ts`。

### 1.5 新手避坑（本次真实踩过的 5 个坑）

1. **锁不存在的行 = gap lock 死锁**：`SELECT ... FOR UPDATE` 打在没有的行上，InnoDB 给的是 gap lock，而 gap lock 之间互相兼容——20 个并发都拿到 gap lock 再一起 INSERT，互等成环集体死锁（1213）。修法：**建行永远在锁行之前**（事务外 upsert，事务内只锁已存在的行）。
2. **Prisma 的死锁错误码会骗人**：普通查询死锁报 P2034，但 **raw SQL（$queryRaw）里的死锁报 P2010 + meta.code="1213"**。重试逻辑只判 P2034 会漏掉一半，两个都要覆盖（幂等单号保证了重试安全）。
3. **Nest 导出 Guard 要连带它的依赖**：AuthModule 只 `exports: [JwtGuard]`，LedgerModule 拿到 JwtGuard 却解析不了它构造函数里的 JwtService（启动即报 UnknownDependenciesException）。要写成 `exports: [JwtModule, JwtGuard]`。
4. **BigInt 不能直接进 JSON**：`JSON.stringify(1n)` 直接抛 TypeError。所有出参经 `toDTO()`/`toString()` 转成字符串，入参用十进制字符串再转 bigint。
5. **bash 的 UID 是只读变量**：脚本里 `UID=$(...)` 直接报 `readonly variable`，改名 LUID。（auth.sh 踩过一次的坑又踩了一遍，记牢。）

## T2 注册 / 登录 / 忘记密码 ✅（2026-09-13，先于 T1 实施）

| 项 | 实际情况 |
|---|---|
| 状态 | ✅ 完成（单测 27/27 + typecheck/lint/build 绿 + L3 `scripts/verify/auth.sh` 8/8）。**新手向实现详解见下方 §2.1~§2.5**：模块分工 → 数据库变更 → 逐文件实现要点 → 逐功能 curl 验证 → 踩坑记录。**函数调用时序图**（注册/登录/忘记密码/鉴权/验证码七张）见 [06-函数调用时序图.md](06-函数调用时序图.md) |
| 已有代码 | User/UserSecurity/SecurityEvent/Kyc 表已建；`packages/shared/src/schemas.ts` 的 registerSchema 已就位；api 的 CORS/PrismaService 就绪 |
| 实际产出 | 数据：`User.inviteCode→invitedBy`、`UserSecurity.tokenVersion`、SecurityEvent 增 userAgent、新增 **MailLog outbox 表**（迁移 `auth_session_and_mail_log`）；共享：login/forgot/reset/registerConfirm 四个 schema + passwordRule；api：`src/auth/`（errors / code.service 频控锁定 / mail.service outbox / totp.ts RFC6238 / jwt.guard tokenVersion 校验 / auth.service / auth.controller / auth.module）+ `redis.service` + `prisma.module(@Global)` + `zod.util`；脚本：`scripts/verify/auth.sh` |
| 关键决策 | 验证码场景 reg/pw 共用频控（60s 重发/日 10 条/错 5 锁 30min，Redis 原子键）；JWT 载荷 `{sub, ver=tokenVersion}`，重置密码 +1 全端失效 + forbiddenUntil 24h；新设备=(IP+UA) 历史未出现；TOTP 自实现零依赖（RFC 6238 向量锚定）；REGION_BLACKLIST 环境变量未配置即关闭 |
| 与计划偏差 | ① 实施顺序：**T2 先于 T1**（登录无资金依赖，先打通账户链路，T1 按计划随后）② 计划落点 `src/verification/` 并入 `src/auth/code.service.ts` ③ tsx 不支持 emitDecoratorMetadata → **api 的 dev 从 tsx 换 ts-node --transpile-only**（matcher/wallet/ws-gateway 不受影响）④ 邮件类型新增 PASSWORD_CHANGED |

### 2.1 模块分工一览（每个功能动的是哪一层）

| 模块 | 本次改动 | 作用 |
|---|---|---|
| `packages/shared/src/schemas.ts` | 新增 4 个校验规则 + 1 条密码规则 | 请求参数怎么算合法（前后端共用同一份） |
| `packages/db/prisma/schema.prisma` | 改 3 处 + 新增 MailLog 表 + 迁移 | 数据存成什么样 |
| `apps/api/src/redis.service.ts` `prisma.module.ts` `zod.util.ts` | 新增 3 个基础设施文件 | 数据库/Redis 连接、参数校验失败的统一出口 |
| `apps/api/src/auth/`（8 个文件） | 新增整个模块 | 全部业务逻辑：注册、登录、忘记密码、2FA、会话 |
| `apps/api/src/app.module.ts` | 挂载 AuthModule | 把模块接进应用 |
| `apps/api/test/` | 新增 auth.spec / totp.spec | 证明逻辑对（不依赖数据库也能跑） |
| `scripts/verify/auth.sh` | 新增 | 证明整个系统连起来对（真实 MySQL/Redis） |
| 根 `.env` | 新增 `REGION_BLACKLIST=""` | 地区黑名单开关（留空=不启用） |

### 2.2 数据库改了什么（先动表，再写代码）

| 改动 | 字段/表 | 为什么 |
|---|---|---|
| User 删 `inviteCode` 改 `invitedBy String?` | 邀请人 uid | 只记「谁邀请的」，M1 不结算返佣 |
| UserSecurity 加 `tokenVersion Int @default(0)` | 会话版本号 | 密码重置后 +1，所有旧 JWT 立即作废（防劫持红线） |
| SecurityEvent 加 `userAgent String?` | 登录日志 | 「新设备」的判断依据 = IP+UA 组合历史上没出现过 |
| 新增 **MailLog** 表 | uid/to/type/payload/status/sentAt | 邮件 outbox：先落库可查不发，M2-5 接真实 SMTP |

```bash
# 执行迁移（在仓库根目录）
pnpm --filter @cex/db exec prisma migrate dev --name auth_session_and_mail_log
# 验证表真的建出来了
docker exec cex-mysql mysql -ucex -pcex_dev_123 -N -s cex -e "SHOW TABLES LIKE 'MailLog'"
```

### 2.3 每个文件写了什么（按这个顺序开发即可）

> 开发顺序 = 依赖顺序：先定校验规则和表 → 再做地基（错误格式/连接）→ 再做纯函数（TOTP）→ 再做验证码/邮件 → 最后组装业务逻辑和路由。

**① 共享校验规则 `packages/shared/src/schemas.ts`**
- 抽出 `passwordRule`（≥8 位、含字母、含数字），注册和重置密码共用，保证两处规则永远一致
- 新增 `registerConfirmSchema`（email+code）、`loginSchema`（email/password/totpCode?）、`forgotPasswordSchema`、`resetPasswordSchema`
- 放 shared 的原因：web 前端将来做表单时 import 同一条规则做即时提示；但后端不信任前端，收到请求仍会重新校验一遍

**② 错误与工具（3 个小文件）**
- `apps/api/src/auth/errors.ts`：`apiError(状态码, 业务码, 中文提示)` 抛 NestJS 的 HttpException。业务码（如 `EMAIL_EXISTS`）给前端分支用，中文提示直接展示给用户
- `apps/api/src/zod.util.ts`：`parseOrThrow(schema, body)` —— zod 校验不通过就抛 400 `VALIDATION`，取第一条错误文案。每个接口开头调用一次，此后代码里 `dto.email` 一定是合法邮箱
- `apps/api/src/redis.service.ts`：ioredis 客户端单例（验证码、频控、锁定计数都在 Redis），进程退出时优雅断开

**③ `apps/api/src/prisma.module.ts`**：`@Global()` 全局模块导出 PrismaService，之后任何业务模块（auth、订单…）直接构造器注入即可，不用每个模块重复声明

**④ 纯函数 `apps/api/src/auth/totp.ts`（2FA 动态码）**
- `base32Decode/base32Encode`：密钥的文本编码（Google Authenticator 扫码用的就是 base32）
- `totpAt(密钥, 时间戳)`：当前 6 位动态码 = 对「时间步计数器 = 秒/30」做 HMAC-SHA1 后动态截断取模
- `totpVerify`：允许 ±1 步（±30 秒）时钟偏差
- 不用第三方库的原因：核心就 20 行，且能用 **RFC 6238 官方测试向量**锚定正确性（官方给定密钥和时刻，断言输出必须是 287082）

**⑤ 验证码 `apps/api/src/auth/code.service.ts`（注册/重置共用）**
- 先定义 `CodeStore` 接口（get/setEx/incr/setNxEx/del 五个方法），再写 Redis 实现 —— **依赖倒置**：业务代码只认接口，单测塞一个内存 Map 实现就能跑，不用起 Redis
- `send`：60 秒重发锁（setNxEx）→ 当日计数（incr，>10 拒绝）→ 生成 6 位随机码存 600 秒
- `verify`：错码计数 ≥5 → 锁 30 分钟（就算之后输对也拒）→ 码不存在=过期 → 码对=删除并清错误计数
- Redis 键名约定 `vc:{场景}:{用途}:{邮箱}`，如 `vc:reg:code:t1@test.local`

**⑥ 邮件 `apps/api/src/auth/mail.service.ts`**：`send(uid, to, type, payload)` 只做一件事 —— 往 MailLog 插一行。M2-5 接 SMTP 时把「落库」升级成「落库+真发」，调用方一行不改

**⑦ 会话守卫 `apps/api/src/auth/jwt.guard.ts`**
- 取 `Authorization: Bearer xxx` → 验签 → 拿 UserSecurity.tokenVersion 和 JWT 里的 ver 比对 → 不一致抛 401 `SESSION_EXPIRED`（重置密码后旧登录全失效就是靠这一步）→ 通过则把 `req.user = { uid }` 传给业务

**⑧ 业务主逻辑 `apps/api/src/auth/auth.service.ts`（核心，5 个方法）**
- `registerInit`：校验 → 地区黑名单 → 邮箱查重（已注册 409）→ 邀请码校验（**无效不阻断**，置空继续）→ 发码+落邮件 → 把「密码哈希+邀请人+地区」存 Redis pending 键（600 秒）
- `registerConfirm`：验码 → 读 pending → 再查重一次（两人同时注册同一邮箱的并发兜底）→ **一个事务**里建 User + UserSecurity → 发欢迎邮件
- `login`：锁定期检查 → bcrypt 比对密码（失败计数，5 次锁 30 分钟；**邮箱不存在也是同一句「邮箱或密码错误」**，防撞库）→ 冻结账号 403 → 开了 2FA 就必须带对动态码 → 查 SecurityEvent 判断新设备（新=记 NEW_DEVICE 事件+发提醒邮件）→ 记 LOGIN 事件 → 清失败计数 → 签发 JWT `{sub: uid, ver: tokenVersion}`
- `forgotPassword`：邮箱不存在**也返回同样的成功文案**（不告诉攻击者哪些邮箱注册过），只是不发码
- `resetPassword`：验码 → 事务：更新密码哈希 + tokenVersion+1 + forbiddenUntil=now+24h → 记 PASSWORD_CHANGED 事件 + 发提醒邮件

**⑨ 路由 `apps/api/src/auth/auth.controller.ts`**：6 个端点 —— `POST /auth/register/init`、`/auth/register/confirm`、`/auth/login`、`/auth/password/forgot`、`/auth/password/reset`、`GET /auth/me`（挂 JwtGuard）。`metaOf()` 从请求头提取 ip（x-forwarded-for）/UA/地区供业务用

**⑩ 装配 `apps/api/src/auth/auth.module.ts` + `app.module.ts`**：JwtModule 用 `process.env.JWT_SECRET`、有效期 7 天；RedisCodeStore/CodeService 需要传参构造，用 `useFactory` 手工装配；app.module 的 imports 里加 `PrismaModule, AuthModule`

### 2.4 每个功能怎么单独验证（起服务后逐步 curl）

> 起服务：`pnpm --filter @cex/api dev`（等日志出现 listening on :4000）。下面每条命令都标了「应该看到什么」，不符合就是实现有问题。

**注册（流程图 1.1）**
```bash
# a. 弱密码 → 应返回 400 {"code":"VALIDATION"}
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/register/init \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"123"}'

# b. 正常发码 → 应返回 200/201 + "验证码已发送"
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/register/init \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"Abcd1234"}'

# c. 频控：60 秒内再发一次 → 应返回 429 {"code":"CODE_TOO_FREQUENT"}
curl -s -X POST localhost:4000/auth/register/init \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"Abcd1234"}'

# d. 从 outbox 读出验证码（应打印 6 位数字）
docker exec cex-mysql mysql -ucex -pcex_dev_123 -N -s cex \
  -e "SELECT JSON_EXTRACT(payload,'$.code') FROM MailLog WHERE \`to\`='t1@test.local' ORDER BY id DESC LIMIT 1"

# e. 错码 → 400 CODE_WRONG；连错 5 次 → 423 CODE_LOCKED
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/register/confirm \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","code":"000000"}'

# f. 正确码 → 应返回 201 {"uid":"...","email":"..."}，User 表多一行
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/register/confirm \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","code":"<d步读到的码>"}'
```

**登录（流程图 1.2）**
```bash
# a. 错密码 → 401 {"code":"INVALID_CREDENTIALS"}；不存在的邮箱也是同样文案
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/login \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"Wrong999"}'

# b. 对密码 → 200 {"token":"eyJ...","user":{...}}，把 token 存下来
curl -s -X POST localhost:4000/auth/login \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"Abcd1234"}'

# c. token 鉴权：不带 token 访问 /auth/me → 401；带上 → 200 返回当前用户
curl -s -w "\n[%{http_code}]\n" localhost:4000/auth/me
curl -s -w "\n[%{http_code}]\n" localhost:4000/auth/me -H "Authorization: Bearer <b步的token>"
# d. 新设备邮件：同一 ip+浏览器第一次登录后，MailLog 应多一条 type=NEW_DEVICE
```

**忘记密码（流程图 1.3）**
```bash
# a. 不存在的邮箱也返回 200 + 同样文案（防枚举），且 MailLog 没有新记录
curl -s -X POST localhost:4000/auth/password/forgot \
  -H 'Content-Type: application/json' -d '{"email":"nobody@test.local"}'

# b. 存在的邮箱 → MailLog 出现 type=RESET_CODE，读出重置码（同注册 d 步）
# c. 重置 → 200；旧 token 立即失效；新密码可登录
curl -s -w "\n[%{http_code}]\n" -X POST localhost:4000/auth/password/reset \
  -H 'Content-Type: application/json' \
  -d '{"email":"t1@test.local","code":"<重置码>","newPassword":"New56789"}'
curl -s -w "\n[%{http_code}]\n" localhost:4000/auth/me -H "Authorization: Bearer <旧token>"   # 应 401 SESSION_EXPIRED
curl -s -X POST localhost:4000/auth/login \
  -H 'Content-Type: application/json' -d '{"email":"t1@test.local","password":"New56789"}'   # 应 200
# d. 数据库核对：UserSecurity.forbiddenUntil 应 = 重置时刻 + 24h（禁提红线）
```

**三层验证总命令（改完代码必跑）**
```bash
pnpm --filter @cex/api test                    # L1 单测：应 27 passed（27/27）
pnpm typecheck && pnpm lint && pnpm build      # L2 工程四件套：全绿无输出即过
pnpm --filter @cex/api dev                     # 起服务
bash scripts/verify/auth.sh                    # L3 全旅程：应打印 🎉 8/8
# push 后 GitHub Actions 自动再验一遍（仓库 Actions 页看绿勾）
```
> `scripts/verify/auth.sh` 的 8 步 = 上面 a~f 的串联版：弱密码 400 → 发码 → 取码 → 错码 400 → 建号 → 错密码 401+登录 → me 鉴权 → 重置+旧 token 失效+禁提标记。新环境跑之前先确认 cex-mysql/cex-redis 容器在跑（`docker ps | grep cex`）。

### 2.5 新手避坑（本次真实踩过的 6 个坑）

1. **dev 用 tsx 起服务会全盘崩**：Nest 靠「装饰器元数据」知道构造器参数是什么来注入依赖，tsx（esbuild）不生成这个元数据 → 报 `Nest can't resolve dependencies`。api 的 dev 改用 `ts-node --transpile-only`（真实 tsc 转换）。matcher/wallet/ws-gateway 不用 Nest，继续 tsx 没问题
2. **Prisma 访问器名 ≠ 关系字段名**：模型叫 `UserSecurity`，代码里写 `prisma.userSecurity`；`include: { security: true }` 里的 security 是 User 上关系字段的名字，两回事
3. **用到的包必须显式声明**：api 直接 import 了 zod，就必须 `pnpm --filter @cex/api add zod`。pnpm 严格 node_modules，不能「恰好装了就用」（本地能跑 CI 报错，因为提升路径不同）
4. **bash 的 `UID` 是只读内建变量**，脚本里赋值直接报错，换成 `NEW_UID`
5. **`VAR=$(func)` 是子 shell**：func 里 `CODE=$(curl ...)` 设置的变量带不回父 shell，验证脚本断言全拿到旧值。解法：把响应码写到临时文件再 cat
6. **CI 是全新环境**：本地能跑不代表 CI 能跑。本项目踩过 `prisma generate`（@prisma/client 要先生成才有 PrismaClient 类型）——已在 ci.yml 的 install 之后补了显式 generate 步骤

## T3 交易对与币种配置 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | PairConfig/ChainConfig/AdminAuditLog 表已建 |
| 实际产出 | —（计划落点：`apps/api/src/admin/`、`packages/db/prisma/seed.ts`） |
| 与计划偏差 | 无 |

## T4 下单前置校验 + 订单 API ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Order/Trade 表已建；`packages/shared/src/schemas.ts` 的 orderRequestSchema（LIMIT 需价格数量/MARKET 需金额的 superRefine 分支）已就位；`@cex/shared` 的 OrderSide/OrderType/OrderStatus 枚举已就位 |
| 实际产出 | —（计划落点：`apps/api/src/order/`） |
| 与计划偏差 | 无 |

## T5 撮合引擎 + 撤单 ⬜（部分骨架）

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未实装（**接口契约已定**） |
| 已有代码 | `apps/matcher/src/orderbook.ts`：OrderInput/PlaceResult/Fill 类型 + OrderBook.place()/cancel()（当前抛「未实现」并指向流程图 3.2）；`apps/matcher/src/index.ts` 健康检查；`apps/matcher` 的 ws 依赖未装（Kafka 依赖 T5 实装时一并加） |
| 实际产出 | —（实装即从本文件替换 orderbook.ts，M0 里那次市价单逻辑的重写按「骨架先行」要求已被搁置） |
| 与计划偏差 | 无 |

## T6 行情 ⬜（部分骨架）

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未实装（订阅协议已通） |
| 已有代码 | `apps/ws-gateway/src/index.ts`：/ws subscribe 协议实测可用（welcome/subscribed 消息）；健康检查含在线客户端数 |
| 实际产出 | —（计划落点：ws-gateway 接 Kafka 行情事件 + api 的 /ticker /klines /depth） |
| 与计划偏差 | 无 |

## T7 充值入账 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Deposit/DepositAddress/ChainConfig/WalletCursor 表已建；`packages/contracts` 的 EVM/Tron 地址校验（8 单测）已就位；wallet 服务骨架+环境自检就绪 |
| 实际产出 | —（计划落点：`apps/wallet/src/`；infra 将追加 anvil + tron-mock 两个 compose 服务） |
| 与计划偏差 | 无 |

## T8 提现 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Withdraw 表（状态机字段）与 ChainConfig（手续费/确认数/开关）已建；contracts 地址校验可复用；24h 禁提/白名单/审核所需表结构已含在 schema |
| 实际产出 | —（计划落点：`apps/api/src/withdraw/`、`apps/wallet/src/broadcast/`） |
| 与计划偏差 | 无 |

## T9 日终对账 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | AssetFlow（不可变流水+快照字段）已建，是三查的数据基础 |
| 实际产出 | —（计划落点：`apps/api/src/recon/` 或独立定时任务脚本） |
| 与计划偏差 | 无 |

## T10 E2E 总演练 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | `scripts/` 目录已存在（当前仅 proxy.sh） |
| 实际产出 | —（计划落点：`scripts/verify/m1-*.sh` 共 10 个脚本） |
| 与计划偏差 | 无 |
