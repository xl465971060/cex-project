# 实际开发图 · M1 核心闭环

> 对应计划：[../开发线路图/01-M1-核心闭环.md](../开发线路图/01-M1-核心闭环.md) ｜ **当前状态：⬜ 未开始**（M0 已预置部分骨架，见各任务「已有代码」）
>
> **维护方式**：每完成一个任务，把「实际产出」从 ⬜ 改为 ✅，登记真实文件路径、关键实现决策、与计划的偏差、CI run 链接。

---

## T1 账务统一入口 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | 数据底座就绪：`packages/db/prisma/schema.prisma` 的 Asset/AssetFlow 表（UNIQUE(bizNo) 已建）；`packages/shared/src/bigint.ts` 精度换算可直接复用 |
| 实际产出 | —（计划落点：`apps/api/src/ledger/` 或独立 `packages/ledger`，开工时定） |
| 与计划偏差 | 无 |

## T2 注册 / 登录 / 忘记密码 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | User/UserSecurity/SecurityEvent/Kyc 表已建；`packages/shared/src/schemas.ts` 的 registerSchema 已就位；api 的 CORS/PrismaService 就绪 |
| 实际产出 | —（计划落点：`apps/api/src/auth/`、`apps/api/src/verification/`、outbox 表待迁移） |
| 与计划偏差 | 无 |

## T3 交易对与币种配置 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | PairConfig/ChainConfig/AdminAuditLog 表已建 |
| 实际产出 | —（计划落点：`apps/api/src/admin/`、`packages/db/prisma/seed.ts`） |
| 与计划偏差 | 无 |

## T4 下单前置校验 + 订单 API ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Order/Trade 表已建；`packages/shared/src/schemas.ts` 的 orderRequestSchema（LIMIT 需价格数量/MARKET 需金额的 superRefine 分支）已就位；`@cex/shared` 的 OrderSide/OrderType/OrderStatus 枚举已就位 |
| 实际产出 | —（计划落点：`apps/api/src/order/`） |
| 与计划偏差 | 无 |

## T5 撮合引擎 + 撤单 ⬜（部分骨架）

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未实装（**接口契约已定**） |
| 已有代码 | `apps/matcher/src/orderbook.ts`：OrderInput/PlaceResult/Fill 类型 + OrderBook.place()/cancel()（当前抛「未实现」并指向流程图 3.2）；`apps/matcher/src/index.ts` 健康检查；`apps/matcher` 的 ws 依赖未装（Kafka 依赖 T5 实装时一并加） |
| 实际产出 | —（实装即从本文件替换 orderbook.ts，M0 里那次市价单逻辑的重写按「骨架先行」要求已被搁置） |
| 与计划偏差 | 无 |

## T6 行情 ⬜（部分骨架）

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未实装（订阅协议已通） |
| 已有代码 | `apps/ws-gateway/src/index.ts`：/ws subscribe 协议实测可用（welcome/subscribed 消息）；健康检查含在线客户端数 |
| 实际产出 | —（计划落点：ws-gateway 接 Kafka 行情事件 + api 的 /ticker /klines /depth） |
| 与计划偏差 | 无 |

## T7 充值入账 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Deposit/DepositAddress/ChainConfig/WalletCursor 表已建；`packages/contracts` 的 EVM/Tron 地址校验（8 单测）已就位；wallet 服务骨架+环境自检就绪 |
| 实际产出 | —（计划落点：`apps/wallet/src/`；infra 将追加 anvil + tron-mock 两个 compose 服务） |
| 与计划偏差 | 无 |

## T8 提现 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | Withdraw 表（状态机字段）与 ChainConfig（手续费/确认数/开关）已建；contracts 地址校验可复用；24h 禁提/白名单/审核所需表结构已含在 schema |
| 实际产出 | —（计划落点：`apps/api/src/withdraw/`、`apps/wallet/src/broadcast/`） |
| 与计划偏差 | 无 |

## T9 日终对账 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | AssetFlow（不可变流水+快照字段）已建，是三查的数据基础 |
| 实际产出 | —（计划落点：`apps/api/src/recon/` 或独立定时任务脚本） |
| 与计划偏差 | 无 |

## T10 E2E 总演练 ⬜

| 项 | 实际情况 |
|---|---|
| 状态 | ⬜ 未开始 |
| 已有代码 | `scripts/` 目录已存在（当前仅 proxy.sh） |
| 实际产出 | —（计划落点：`scripts/verify/m1-*.sh` 共 10 个脚本） |
| 与计划偏差 | 无 |
