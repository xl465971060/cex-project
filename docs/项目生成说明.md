# 项目生成说明与验证手册

> 生成日期：2026-09-13 ｜ 基准提交：`fb7d4b6`（main 分支首个提交）
>
> **本文档回答一个问题：项目里每一个「可以验证的东西」，怎么验证、预期看到什么、验证没通过怎么办。**
> 包括你点名的：GitHub Actions CI 文件怎么证明它没问题，其余每项（工具链、容器、每个包、每个服务、数据库迁移、前端页面）也都有一一对应的验证步骤。所有「实测结果」均为本机真实执行记录，非理论值。

---

## 0. 生成范围声明（重要）

按当前阶段要求，本次生成的是**基本项目框架（骨架）**，不是完整业务代码：

| 类别 | 本次已生成 | 明确留给后续里程碑 |
|---|---|---|
| 工程骨架 | monorepo 根配置、9 个子包、全部可启动 | — |
| 各服务入口 | 健康检查 + 环境自检（matcher 额外有撮合接口骨架定义） | 注册/登录/下单/提现等业务接口（M1 起） |
| 数据库 | Prisma schema 15 张表 + 初始迁移（已应用到 MySQL） | 业务读写逻辑 |
| 前端 | web/admin 各一个可启动页面（证明环境就绪） | 行情/交易/资产/Admin 六页面（M3） |
| CI | GitHub Actions：lint→typecheck→test→build | 集成测试 job（需要容器，见 §6.1）、镜像构建发布 |

业务功能的完整设计不在本文档重复，见：
- 功能范围：《[交易所MVP-做与不做及最小流程.md](./交易所MVP-做与不做及最小流程.md)》
- 程序流程图：《[MVP功能程序流程图.md](./MVP功能程序流程图.md)》
- 架构与选型：《[技术决策方案-架构与选型.md](./技术决策方案-架构与选型.md)》

---

## 1. 目录结构与职责

```
prep-cex/
├── .github/workflows/ci.yml      # GitHub Actions CI（替代原 GitLab CI，已删除 .gitlab-ci.yml）
├── apps/                         # 6 个可独立启动的应用
│   ├── api/          :4000  NestJS 11   REST 网关（健康检查已验证真实连 MySQL）
│   ├── matcher/      :4001  Node+ts     撮合引擎（骨架：OrderBook 接口定义，逻辑 M2 填充）
│   ├── wallet/       :4002  Node+ts     钱包服务（骨架：健康检查 + 环境自检）
│   ├── ws-gateway/   :4003  Node+ws     WebSocket 推送（骨架：订阅协议已可用）
│   ├── web/          :3000  Next.js 15  用户前端（骨架首页）
│   └── admin/        :3001  Next.js 15  管理后台（骨架首页）
├── packages/                     # 3 个被应用共享的基础包
│   ├── shared/                   # 枚举 / bigint 精度换算 / zod 请求校验（17 个单测中占 8）
│   ├── db/                       # Prisma schema 15 表 + 迁移 + 单例 Client
│   └── contracts/                # EVM/Tron 地址校验（viem + base58check，8 个单测）
├── infra/docker-compose.dev.yml  # MySQL 8.4 + Redis 7 + Kafka 3.9（KRaft 单节点）
├── docs/                         # 5 份设计与本文档
├── package.json / pnpm-workspace.yaml / turbo.json   # monorepo 根：pnpm 9.15.0 + Turborepo v2
├── tsconfig.base.json / eslint.config.mjs / .gitignore / .env.example
└── pnpm-lock.yaml                # 锁定文件（CI 用 --frozen-lockfile 依赖它）
```

约定：
- 端口统一从 `@cex/shared` 的 `SERVICE_PORTS` 读取，`.env` 可覆盖；
- 服务金额一律 bigint 最小单位（红线，见 shared/bigint.ts）；
- `packages/*` 编译为 CJS `dist`，被 NestJS/tsx/Next 三方共同消费。

---

## 2. 逐项验证手册（每项 = 验证什么 + 命令 + 预期 + 实测）

> 以下命令默认在项目根目录 `/home/administrator/tstClaude/prep-cex` 执行。

### 2.1 基础工具链

| 验证什么 | 命令 | 通过标准 | 实测结果 |
|---|---|---|---|
| Node 22 | `node -v` | `v22.x` | v22.23.2 ✅ |
| pnpm 9 | `pnpm -v` | `9.15.0` | 9.15.0 ✅ |
| git | `git --version` | 有输出 | git version 2.43.0 ✅ |
| docker | `docker --version` | 有输出 | Docker version 29.8.0 ✅ |
| 依赖可安装 | `pnpm install` | `Done in ...` 无 ERR | 346 个包，Done in 1m31.5s ✅ |

依赖安装失败排查：先 `ping registry.npmjs.org`；公司网络需配置镜像：`pnpm config set registry https://registry.npmmirror.com`。

### 2.2 基础设施容器（充值/交易/提现的数据底座）

```bash
docker compose -f infra/docker-compose.dev.yml up -d   # 启动（已启动则跳过）
docker ps --format '{{.Names}}\t{{.Status}}' | grep cex
```

通过标准与实测（三项全部出现且 healthy）：

```
cex-redis    Up ... (healthy) ✅
cex-mysql    Up ... (healthy) ✅
cex-kafka    Up ...           ✅（kafka 镜像无内置 healthcheck，用下面命令单独验证）
```

| 组件 | 单独验证命令 | 通过标准 |
|---|---|---|
| MySQL | `docker exec cex-mysql mysqladmin -ucex -pcex_dev_123 ping` | `mysqld is alive` |
| Redis | `docker exec cex-redis redis-cli ping` | `PONG` |
| Kafka | `docker exec cex-kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list` | 命令正常返回（空列表也算通过） |

### 2.3 packages/shared（精度红线所在包，必须最先验证）

```bash
pnpm --filter @cex/shared build && pnpm --filter @cex/shared test
```

通过标准：`tsc` 无报错；vitest 报告全绿。实测：**Test Files 1 passed / Tests 8 passed**。
这 8 个用例覆盖：bigint 进出精度（toBaseUnits/formatBaseUnits）、非法数字拒绝、zod 订单/提现/注册 schema 分支。

### 2.4 packages/db（数据库模块，4 步验证）

```bash
# ① schema 合法性（不需要数据库在跑）
pnpm --filter @cex/db exec prisma validate
# 预期：The schema at prisma/schema.prisma is valid 🚀

# ② 生成客户端（所有服务的 ORM 代码）
pnpm --filter @cex/db exec prisma generate
# 预期：✔ Generated Prisma Client

# ③ 迁移应用到 MySQL（需要 MySQL 容器运行）
pnpm --filter @cex/db exec prisma migrate dev --name init
# 预期：Your database is now in sync with your schema.

# ④ 表真的存在（眼见为实）
docker exec cex-mysql mysql -ucex -pcex_dev_123 cex -e "SHOW TABLES;"
```

④ 的实测输出（15 张业务表 + 1 张迁移记录表）：

```
AdminAuditLog, Asset, AssetFlow, ChainConfig, Deposit, DepositAddress,
Kyc, Order, PairConfig, SecurityEvent, Trade, User, UserSecurity,
WalletCursor, Withdraw, _prisma_migrations
```

> **已踩过的坑（P3014）**：业务账号 `cex` 无建库权限，`prisma migrate dev` 需要 shadow database。
> 解决：用 root 建 `cex_shadow` 库并授权给 cex，schema 里声明了 `shadowDatabaseUrl`（见 schema.prisma datasource 段）。换新机器重建环境时，要先执行：
> ```sql
> CREATE DATABASE IF NOT EXISTS cex_shadow;
> GRANT ALL PRIVILEGES ON cex_shadow.* TO 'cex'@'%';
> GRANT ALL PRIVILEGES ON cex.* TO 'cex'@'%';
> FLUSH PRIVILEGES;
> ```
> 生产/CI 用 `prisma migrate deploy` 不需要 shadow 库。

### 2.5 packages/contracts（链上地址校验）

```bash
pnpm --filter @cex/contracts build && pnpm --filter @cex/contracts test
```

实测：**8 passed**。用例覆盖 EVM 地址（含 EIP-55 大小写校验和）、Tron base58check（双 SHA256 校验和）、非法输入拒绝。充值地址校验直接复用这套函数，所以单测过了 = 充值地址校验的"零件"可用。

### 2.6 整个 monorepo 的四条命令（CI 里跑的就是它们）

```bash
pnpm typecheck   # 预期：Tasks: 12 successful（实测 ✅ 12/12，5 cached）
pnpm test        # 预期：Tasks: 11 successful（实测 ✅ shared 8 + api 1 + contracts 8 = 17 用例）
pnpm build       # 预期：Tasks: 9 successful（实测 ✅ 含 web/admin 两次 next build，约 20s）
pnpm lint        # 预期：无任何输出即通过（实测 ✅ 0 error 0 warning）
```

任何一个失败，turbo 会明确打出失败的包名（如 `@cex/contracts#typecheck: ERROR`），先看它的输出。

### 2.7 apps/api（NestJS 网关 —— 启动即验证数据库连通）

```bash
pnpm --filter @cex/api dev        # 终端 1
curl -s localhost:4000/health     # 终端 2
```

实测响应：

```json
{"service":"api","status":"ok","time":"2026-09-13T05:07:36.236Z"}
```

**为什么这一条同时证明了 MySQL 连通**：`PrismaService.onModuleInit()` 会 `await this.$connect()`——数据库连不上，服务根本起不来，`/health` 不会返回 200。所以：health 返回 ok = NestJS 依赖注入、Prisma Client、MySQL 网络+账号+密码，四件事全部正常。

另有两个公开接口：`GET /pairs`（交易对列表，当前从代码返回骨架数据）、`GET /`（版本信息）。

### 2.8 apps/matcher（撮合引擎骨架）

```bash
pnpm --filter @cex/matcher dev
curl -s localhost:4001/health
```

实测：`{"service":"matcher","status":"ok","pair":"BTC/USDT",...}`
撮合逻辑本身是骨架（`orderbook.ts` 定义了 `OrderInput/PlaceResult` 接口，`place()` 抛"未实现"并指向流程图 §3.2）。M2 实现时，验证标准预先写好：**流程图 3.2 的全部情况编号 = 单测用例清单**，价格-时间优先、市价单按金额、自成交拒绝、撤单解冻。

### 2.9 apps/wallet（环境自检是它的核心骨架功能）

```bash
pnpm --filter @cex/wallet dev
curl -s localhost:4002/health | python3 -m json.tool
```

实测（这就是**设计行为**，不是故障）：

```json
"env": [
  { "key": "DATABASE_URL",       "ready": true  },   ← 根 .env 已提供
  { "key": "WALLET_ENC_KEY",     "ready": false },   ← 待配置（热钱包加密密钥）
  { "key": "TRONGRID_API_KEY",   "ready": false },   ← 待配置（TronGrid 注册后）
  { "key": "EVM_RPC_URL",        "ready": false }    ← 待配置（EVM 节点/RPC 服务）
]
```

`ready:false` 的项就是上线前必须补的配置清单，补齐后重启服务应全部变 true。

### 2.10 apps/ws-gateway（HTTP 健康 + WS 功能两个都要验）

```bash
pnpm --filter @cex/ws-gateway dev
curl -s localhost:4003/health          # ① HTTP 健康，实测 ok
# ② WebSocket 订阅功能（用 node 起一个临时客户端；Ctrl+C 退出）
pnpm --filter @cex/ws-gateway exec node -e "
const WebSocket = require('ws');
const ws = new WebSocket('ws://localhost:4003/ws');
ws.on('message', d => { console.log('收到:', d.toString()); });
ws.on('open', () => ws.send(JSON.stringify({action:'subscribe',channels:['ticker.BTC/USDT']})));
setTimeout(()=>process.exit(0), 2000);"
```

② 的实测输出（两条都收到 = 通过）：

```
收到: {"type":"welcome","message":"ws-gateway 骨架已连接"}
收到: {"type":"subscribed","channels":["ticker.BTC/USDT"]}
```

### 2.11 apps/web 与 apps/admin（Next.js 前端）

```bash
pnpm --filter @cex/web dev    # 终端 1
pnpm --filter @cex/admin dev  # 终端 2
curl -s -o /dev/null -w "web   HTTP %{http_code}\n" localhost:3000   # 预期 200
curl -s -o /dev/null -w "admin HTTP %{http_code}\n" localhost:3001   # 预期 200
```

实测：两者均 **HTTP 200**，页面内容分别含「CEX 交易所 - 用户端骨架」「CEX Admin - 管理后台骨架」。
注意 `pnpm build` 已经验证过两次 `next build` 成功，运行时 curl 是再验一遍"能起服务、能渲染"。

### 2.12 服务的启动与停止（运维操作的正确姿势）

```bash
# 启动全部（推荐）：turbo 按依赖顺序先构建包，再并行拉起 6 个应用
pnpm dev

# 停止：在 pnpm dev 的终端 Ctrl+C（turbo 会连带停掉子进程）
# 若有残留进程占用端口，按端口精确清理（不要用 pkill -f 模糊匹配，容易误杀）：
ss -ltnp | grep -E ':(300[01]|400[0-3])\s'     # 找到 pid=N
kill -9 <pid>                                   # 逐个清理
```

---

## 3. 一键全量验证（新机器/改完代码后的标准流程）

```bash
git status                                   # 0. 确认代码就位
pnpm install                                 # 1. 依赖
docker compose -f infra/docker-compose.dev.yml up -d   # 2. 基础设施
cp .env.example .env                         # 3. 环境变量（已有则跳过）
pnpm --filter @cex/db exec prisma migrate deploy       # 4. 数据库迁移（幂等，已应用则跳过）
pnpm lint && pnpm typecheck && pnpm test && pnpm build # 5. 质量四连
pnpm dev                                     # 6. 起服务，按 §2.7-2.11 逐个 curl
```

1-5 步全绿 + 6 步 6 个端口有响应 = 这套骨架完全可用。

---

## 4. GitHub Actions CI：怎么证明这个 CI 文件没问题

> 你点名要的部分。背景：项目原先写的是 GitLab CI（`.gitlab-ci.yml`），**已删除**；现使用 `.github/workflows/ci.yml`，分三层验证，一层比一层接近真实。

CI 文件内容概要（`.github/workflows/ci.yml`）：触发条件 `push 到 main/develop` 或任意 `pull_request`；单个 job `verify` 跑在 `ubuntu-latest`；步骤 = checkout → pnpm（版本自动读根 package.json 的 `packageManager: pnpm@9.15.0`）→ Node 22（带 pnpm 缓存）→ `pnpm install --frozen-lockfile` → lint → typecheck → test → build。与本地 §2.6 四条命令完全一致，**本地绿 = CI 绿**，不会有"本地好好的 CI 挂了"这种环境差异。

### 第一层：静态校验（30 秒，push 前就该跑）

用 **actionlint**——GitHub Actions 生态的标准校验器，能查出：YAML 语法错误、非法字段名（如把 `runs-on` 写错）、step 里引用不存在的 input、shell 脚本低级错误。

```bash
# 安装（一次性；Docker Hub 拉镜像慢时的备选方案就是官方安装脚本）
curl -sL https://github.com/rhysd/actionlint/releases/download/v1.7.7/actionlint_1.7.7_linux_amd64.tar.gz | tar xz -C /tmp actionlint
# 校验
/tmp/actionlint .github/workflows/ci.yml
```

**通过标准：无任何输出 + exit 0。实测：v1.7.7 校验本仓库 ci.yml 通过 ✅**
（改过 ci.yml 之后必须重跑这一条。）

另一层保底：YAML 语法本身可以 `pnpm dlx js-yaml .github/workflows/ci.yml` 验证能被解析。

### 第二层：本地完整演练（可选，15 分钟）

用 [act](https://github.com/nektos/act) 在本地 Docker 里把整个 workflow 真跑一遍：

```bash
pnpm dlx @github-local/act -j verify      # 或 brew install act / 官方二进制
```

通过标准：`✔ Success - verify ...`。适合 CI 挂了但看不出来为什么的时候本地复现。注意 act 对 `actions/setup-node` 的缓存特性支持不完整，缓存相关告警可忽略。

### 第三层：推送到 GitHub 后的真实验证（最终标准）

```bash
# 首次推送（见 §5 完整步骤）
git remote add origin git@github.com:<你的用户名>/prep-cex.git
git push -u origin main

# 然后任选其一观察：
# a) 网页：仓库页 → Actions 标签页 → 点最新一次 run
#    通过标准：verify job 全部步骤绿勾，总耗时约 2-4 分钟
# b) 命令行（装了 gh CLI）：
gh run list --limit 1          # 通过标准：✓ main ... success
gh run watch                   # 实时看进度
gh run view --log-failed       # 失败时：只看失败步骤的日志
```

实测状态：本机已完成第一层（actionlint 通过）；第三层待仓库推送到 GitHub 后自然完成——**这正是 CI 设计的目的：每次 push 自动执行**。

### CI 失败速查表

| 现象 | 原因 | 处理 |
|---|---|---|
| `Lockfile does not match` | 改了 package.json 没更新锁文件 | 本地重跑 `pnpm install`，提交 pnpm-lock.yaml |
| setup-node 步骤超时 | GitHub↔npm 网络抖动 | 重跑（Re-run failed jobs） |
| lint 失败但本地过了 | 本地没提交新文件，或 eslint 版本差异 | 本地 `pnpm lint` 确认后再 push |
| prisma generate 失败 | schema 语法错误 | 本地 `prisma validate` 先验 |
| build 时 next 报类型错 | Next 版本升级引入新检查 | 按 CI 日志逐条修，CI 的 TypeScript 就是标准答案 |

---

## 5. 推送到 GitHub 的步骤

```bash
# 0. 本仓库 git 身份当前是本地占位（administrator@localhost），推 GitHub 前改成你自己的：
git config user.name  "<你的GitHub用户名>"
git config user.email "<GitHub邮箱>"

# 1. 在 GitHub 网页创建空仓库 prep-cex（不要勾选任何初始化 README/.gitignore）

# 2. 关联远程并推送
git remote add origin git@github.com:<你的用户名>/prep-cex.git
git push -u origin main

# 3. 推完立即按 §4 第三层验证 CI
```

SSH key 没配的话：`ssh-keygen -t ed25519` → 把 `~/.ssh/id_ed25519.pub` 内容贴到 GitHub → Settings → SSH and GPG keys。
改完 git 身份后，如果想把首个提交的作者也改掉：`git commit --amend --reset-author`，再强制推送（仓库仅自己可见时安全）。

---

## 6. 已知边界与后续接入点（防止误解骨架的行为）

### 6.1 CI 目前没有 MySQL/Redis 容器

现有 17 个单测全部是纯逻辑测试，不需要数据库，所以 CI 不起容器（更快更稳）。**当 M1 引入需要真实 MySQL 的集成测试时**，在 verify job 里加：

```yaml
services:
  mysql:
    image: mysql:8.4
    env:
      MYSQL_ROOT_PASSWORD: cex_dev_root
      MYSQL_DATABASE: cex
    options: >-
      --health-cmd="mysqladmin ping -uroot -pcex_dev_root"
      --health-interval=10s --health-timeout=5s --health-retries=5
    ports: ["3306:3306"]
```

### 6.2 环境变量加载机制（谁读哪个文件）

| 文件 | 谁读 | 说明 |
|---|---|---|
| 根 `.env`（已 gitignore） | 4 个 Node 服务经 `dotenv-cli -e ../../.env` 读 | dev 脚本已配好，换目录执行不会找错文件 |
| `packages/db/prisma/.env`（已 gitignore） | Prisma CLI（validate/migrate/studio） | monorepo 下 Prisma CLI 不读根 .env，需单独放 |
| 真实环境变量 | 生产容器 | 生产不用 .env 文件，K8s/Docker 注入 |

> 踩坑记录：曾出现 wallet 自检报 `DATABASE_URL ready:false`——原因就是 `pnpm --filter` 在子包目录执行，dotenv 相对路径失效；已用 dotenv-cli 显式指到根 `.env` 修复，并用 §2.9 的自检接口验证修复生效。这正是环境自检接口存在的意义。

### 6.3 matcher 的撮合逻辑是接口骨架

`apps/matcher/src/orderbook.ts` 定义了 `OrderBook.place()/cancel()` 的输入输出类型，`place()` 目前抛出"未实现"并指向流程图。M2 按《MVP功能程序流程图.md》§3.2 的每个情况编号写单测后填实现——骨架已保证类型契约先行。

### 6.4 前端暂未接 API

web/admin 首页目前是静态文案。M3 接入时：API 已开 CORS（api/src/main.ts），web 走 `NEXT_PUBLIC_API_URL` 调 :4000。
