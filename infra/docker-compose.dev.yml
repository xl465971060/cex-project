# 开发环境基础设施：MySQL 8 + Redis 7 + Kafka(KRaft)
# 启动:   docker compose -f infra/docker-compose.dev.yml up -d
# 停止:   docker compose -f infra/docker-compose.dev.yml down
# 清数据: docker compose -f infra/docker-compose.dev.yml down -v

name: cex-dev

services:
  mysql:
    image: mysql:8.4
    container_name: cex-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: cex_dev_root
      MYSQL_DATABASE: cex
      MYSQL_USER: cex
      MYSQL_PASSWORD: cex_dev_123
    ports:
      - "3306:3306"
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - cex_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-pcex_dev_root"]
      interval: 5s
      timeout: 3s
      retries: 30

  redis:
    image: redis:7-alpine
    container_name: cex-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - cex_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 30

  kafka:
    image: apache/kafka:3.9.0
    container_name: cex-kafka
    restart: unless-stopped
    ports:
      - "9092:9092"
    environment:
      # 单机 KRaft 模式（免 ZooKeeper）
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    volumes:
      - cex_kafka_data:/var/lib/kafka/data

volumes:
  cex_mysql_data:
  cex_redis_data:
  cex_kafka_data:
